sudo: required
services:
  - docker

before_install:
  - docker build -t EtienneRQT/docker-react -f Dockerfile.dev .


script:
  - docker run -e CI=true EtienneRQT/docker-react npm run test

language: generic 